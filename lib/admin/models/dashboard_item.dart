import 'package:flutter/material.dart';

class DashboardIcon{
  final  IconData iconData;
  final String text;
   bool isSelected;
  DashboardIcon({required this.iconData,required this.text,required this.isSelected});
}